# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Secrets & local config — NEVER commit API keys (PRD §1.5 / §11)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build orchestration scratch (not part of the product)
.peeky-build-workflow.js

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Internal product spec (private infra / contacts) — not for the public repo
prd_screen_companion.md

# Claude Code local settings (per-machine, not for the repo)
.claude/settings.local.json
