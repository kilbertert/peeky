# Peeky local secrets — copy to .env and fill in. NEVER commit the real .env.
# The API key can be provided here (read at runtime as PEEKY_API_KEY) instead of
# storing it in the settings config file. See PRD §1.5 / §11 security red-lines.
PEEKY_API_KEY=
